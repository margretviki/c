#inclue<stdio.h>
int main()
int num,sum=0;
{
printf("enter the sum of it digits\n");
scanf("%d",&num);
for(i=o;i<num;i++)
{
sum+=num%0;
num+=sum=0;
printf("enter the number of digits %s"\num)
}
return 0;
}
