#include<stdio.h>
 float n = 1.5;
int main;
{
      float b;

       printf("Please enter a value to be rounded: ");
       scanf("%f", &n);

       n = n + add;
       n = (int)n;

       printf("The rounded value is %0.8", n);

      return 0;
}
